@model HomeViewModel

<h1>Лучшие автомобили</h1>
<div class="row mt-5 mb-2">
    @{
        foreach (var car in Model.favCars) { 
            @Html.Partial("CarTechnology", car)
        }
    }
</div>